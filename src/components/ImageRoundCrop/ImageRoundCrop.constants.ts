import {SCREEN_WIDTH} from '../../constants/layout';

export const CROP_OFFSET = 16;
export const CROP_SIZE = SCREEN_WIDTH - CROP_OFFSET * 2;

export const CROP_CONTAINER_WIDTH = SCREEN_WIDTH;
export const CROP_CONTAINER_HEIGHT = SCREEN_WIDTH;

export const MIN_IMAGE_SCALE = 1;
export const MAX_IMAGE_SCALE = 3;
